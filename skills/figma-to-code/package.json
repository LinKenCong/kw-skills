{
  "name": "figma-to-code",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check bridge.mjs && node --check scripts/bridge_client.mjs && node --check scripts/query.mjs && node --check plugin/code.js"
  },
  "dependencies": {
    "pixelmatch": "^6.0.0",
    "pngjs": "^7.0.0",
    "puppeteer": "^24.0.0",
    "sharp": "^0.33.0"
  }
}
